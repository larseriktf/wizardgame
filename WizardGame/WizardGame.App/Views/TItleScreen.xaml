<Page
    x:Class="WizardGame.App.Views.TitleScreen"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Style="{StaticResource PageStyle}"
    mc:Ignorable="d">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="512" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="256" MaxHeight="256" />
            <RowDefinition Height="auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Image Style="{StaticResource TitleScreenBG}"
               Grid.ColumnSpan="3"
               Grid.RowSpan="3"
               Source="/Assets/Images/TitleScreen/fantasyLand.jpg"/>

        <TextBlock Style="{StaticResource MainTitle}"
                   Grid.Column="0" Grid.ColumnSpan="3"
                   Text="Phantastic Wizard"/>

        <Grid Grid.Column="1" Grid.Row="1" Background="Indigo">
            <StackPanel Style="{StaticResource MenuStackPanel}" Orientation="Vertical">
                <Button Content="Start new game"
                        Click="OnClickToStartGame"
                        Margin="0 0 0 32"
                        HorizontalAlignment="Center"/>
                <Button Content="Continue game"
                        Click="OnClickToContinueGame"
                        Margin="0 0 0 32"
                        HorizontalAlignment="Center"/>
                <Button Content="Spell book"
                        Click="OnClickToEnterSpellBook"
                        Margin="0 0 0 32"
                        HorizontalAlignment="Center"/>
                <Button Content="Settings"
                        Click="OnClickToEnterSettings"
                        Margin="0 0 0 32"
                        HorizontalAlignment="Center"/>
                <Button Content="Quit game"
                        Click="OnClickToQuitGame"
                        HorizontalAlignment="Center"/>
            </StackPanel>
        </Grid>
        

    </Grid>
</Page>
