<Page
    x:Class="WizardGame.App.Views.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Style="{StaticResource PageStyle}"
    mc:Ignorable="d">


    <Grid Background="Indigo">
        <StackPanel>
            <TextBlock Style="{StaticResource MainTitle}"
                   Grid.Column="0" Grid.ColumnSpan="3"
                   Text="Settings"/>

            <Pivot>
                <PivotItem Header="General">
                    <!-- Pivot content here -->
                    <StackPanel>
                        <Grid Style="{StaticResource SettingsRow}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition/>
                                <ColumnDefinition/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0" Text="Setting 1"/>
                            <TextBlock Grid.Column="1" Text="Option / Option"/>
                        </Grid>
                        <Grid Style="{StaticResource SettingsRow}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition/>
                                <ColumnDefinition/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0" Text="Setting 2"/>
                            <TextBlock Grid.Column="1" Text="Option / Option"/>
                        </Grid>
                        <Grid Style="{StaticResource SettingsRow}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition/>
                                <ColumnDefinition/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0" Text="Setting 3"/>
                            <TextBlock Grid.Column="1" Text="Option / Option"/>
                        </Grid>
                    </StackPanel>

                </PivotItem>
                <PivotItem Header="Keybindings">
                    <!-- Pivot content here -->
                    <ScrollViewer>
                        <StackPanel>
                            <Grid Style="{StaticResource SettingsRow}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition/>
                                    <ColumnDefinition/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Grid.Column="0" Text="Move Left"/>
                                <StackPanel Grid.Column="1" Orientation="Horizontal">
                                    <TextBlock Text="A"/>
                                    <TextBlock Text="Change"
                                                   Margin="10 0 0 0">
                                    </TextBlock>
                                </StackPanel>
                            </Grid>
                        </StackPanel>
                    </ScrollViewer>
                </PivotItem>
                <PivotItem Header="Configurations">
                    <!-- Pivot content here -->
                    <ScrollViewer>
                        <StackPanel>
                            <GridView x:Name="ConfigurationsGridView"
                                          
                                          ItemClick="OnInspectConfiguration"
                                          IsItemClickEnabled="True"
                                          SelectionMode="Single"
                                          />
                            <!--

                                ItemTemplate="{StaticResource ConfigurationsTemplate}"

                                <DataTemplate x:Key="ConfigurationsTemplate">
                                    <TextBlock Text="Bruh"/>
                                </DataTemplate>

                                -->
                        </StackPanel>
                    </ScrollViewer>
                </PivotItem>
            </Pivot>
        </StackPanel>
    </Grid>
</Page>
